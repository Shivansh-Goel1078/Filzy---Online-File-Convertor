import { Routes } from '@angular/router';
import { FileUploadComponent } from './Components/file-upload/file-upload.component';

export const routes: Routes = [
    {
        path:'File-upload',
        component: FileUploadComponent,
        title: "File Upload | Filzy"
    }
];
